// Crie um script que inicialize 3 variáveis numéricas, compare o valor
// dos 3 e mostre o maior.

var numeroA = 1;
var numeroB = 5;
var numeroC = 3;

if (numeroA > numeroC && numeroA > numeroB) {
  console.log(numeroA + " " + "é o maior!");
} else if (numeroC > numeroA && numeroC > numeroB) {
  console.log(numeroC + " " + "é o maior!");
} else {
  console.log(numeroB + " " + "é o maior!");
}
